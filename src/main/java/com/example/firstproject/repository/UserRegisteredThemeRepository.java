package com.example.firstproject.repository;

//import com.example.firstproject.model.UserRegisteredTheme;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

import javax.transaction.Transactional;
import java.util.List;

/*@Repository
public interface UserRegisteredThemeRepository extends JpaRepository<UserRegisteredTheme, Integer> {

    List<UserRegisteredTheme> findAllByUserId(String userId);

    @Transactional
    void deleteByThemeId(Integer themeId);
}*/
